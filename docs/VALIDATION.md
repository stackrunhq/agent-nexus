# 第一阶段验证记录

## 显式调用任务关联（步骤 52，2026-09-16）

- 原生 PostgreSQL 17.11 / pgvector 0.8.6 全量 171 passed、0 skipped，115.95 秒，包含备份恢复；临时实例及生成凭据已清理。
- 新定向 2 passed，验证重复请求 ID 的两个后台任务仍精确隔离、未关联请求匹配单独标记、同步构建记录任务 ID、跨企业关联拒绝，以及 0015→0016 保留旧 pending 调用且不回填。
- 前端 33 passed，逐行 exact/request_match 标签与原有详情分页通过；TypeScript/Vite、Ruff、Git 空白检查通过。
- 清理 build 后构建并隔离安装 wheel，0016、页面资源字节一致、上传解析、混合检索、引用问答、配额和 Worker 启停验证通过。
- 未运行 Docker 容器或浏览器视觉验收；保留依赖弃用及 Vite 大文件提示。目前任务关联不区分尝试次数/批次，未关联调用仍可能匹配不唯一。

## 管理员任务详情（步骤 51，2026-09-15）

- 原生 PostgreSQL 17.11 / pgvector 0.8.6 全量 169 passed、0 skipped，129.72 秒；临时实例停止并删除生成凭据。后端详情/历史定向 4 passed。
- 测试覆盖任务范围、跨企业 404、无凭据 401、参数 422、关联请求/能力过滤、分页以及排除 claim_token 和模型 fingerprint。
- 前端 33 passed，详情分页、未知 token 和关闭取消请求通过；TypeScript/Vite、Ruff、Git 空白检查通过。
- 清理 build 后重建 wheel 并隔离安装，0015、前端资源字节一致、知识库上传解析/检索/引用问答、配额及 Worker 启停验证通过。
- 未执行 Docker 容器或浏览器视觉验收；保留依赖弃用和 Vite 大文件提示。关联仅按请求等条件匹配，不保证精确任务归属，详见 INDEX_DETAILS。

## 检查点混合读写（步骤 50，2026-09-15）

- 原生 PostgreSQL 17.11 隔离数据库最终十轮：四读一写、两连接零溢出，480 次读取、40 次写事务，所有状态/批次、提交后精确计数及租户隔离校验通过；被修改任务最终检查点与批次均为 0。
- 真实执行生产检查点保存和 finish 清理，基准用屏障协调阶段；没有调用模型或生产重试入口。原始样本和计时范围见 HISTORY_READ_WRITE。
- 历史查询与检查点定向 8 passed，Ruff 和 Git 空白检查通过。重建 wheel 并隔离安装验证 0015、资源一致、知识库上传解析/检索/引用问答、配额与 Worker 启停通过。
- 本轮只扩展基准，未重跑前端或后端全量测试；未验证真实网络故障、Worker 崩溃、Docker 或浏览器。保留依赖弃用提示，临时数据库/实例与凭据已清理。

## 多租户检查点历史查询（步骤 49，2026-09-15）

- 原生 PostgreSQL 17.11 / pgvector 0.8.6 后端全量 168 passed、0 skipped，130.81 秒。临时实例与生成凭据已清理。
- 四租户各 2000 任务、500 个带检查点任务、每任务八批；优化前后两轮分别 1/4 线程，共 100 次读取全部通过任务 ID 隔离及批次数精确校验。真实计划由扫描 16000 批次转为 21 次按任务主键统计。
- 原有 20000 条单租户六组 offset/游标基准复跑通过；Ruff、Git 空白检查通过。清理 build 后重新构建并隔离安装 wheel，0015、资源字节一致、上传解析/检索/引用问答、配额和 Worker 启停通过。
- 新基准首次运行发现合成租户摘要重复，修正为唯一值后重跑通过；失败实例也已清理。
- 前端未修改，未重跑前端测试；未运行 Docker、浏览器视觉验收或读写同时竞争。本轮仅测合成元信息和静态数据，保留依赖弃用提示。

## 历史查询性能（步骤 48，2026-09-15）

- 原生 PostgreSQL 17.11 优化前后各生成 20000 条合成任务，六组查询各预热一次、测量五次，均校验 offset/游标与预期任务 ID 一致；保存完整 EXPLAIN ANALYZE/BUFFERS JSON。临时数据库、实例和凭据已清理。
- 版本/模型游标过滤前序行数由 16000/8000 降至 1；详细耗时与限制见 HISTORY_BENCHMARK，不据短测声称所有筛选均提速。
- 后端历史与任务定向测试 7 passed，Ruff、Git 空白检查通过。重建并隔离安装 wheel，0015、页面资源一致性、知识库上传解析/检索/引用问答、配额和 Worker 启停通过。
- 本轮未修改前端或迁移，未重跑前端和后端全量回归；未运行 Docker、浏览器视觉验收、冷缓存或并发查询基准。后端保留既有依赖弃用提示。

## 游标分页与查询索引（步骤 47，2026-09-15）

- 原生 PostgreSQL 17.11 / pgvector 0.8.6 全量 168 passed、0 skipped，130.72 秒；临时实例停止，生成凭据已删除。
- 定向 3 passed：筛选分页、持续插入、同秒排序、锚点删除、查询条件绑定、非法游标和 0014→0015 索引迁移。
- 前端全量 31 passed；TypeScript/Vite 最终生产构建、Ruff、Git 空白检查通过。页面测试验证续查携带 cursor、筛选清空 cursor 及请求取消。
- 清理构建目录后生成 wheel，隔离安装验证 0015、资源字节一致、上传解析、混合检索、引用问答、配额和 Worker 启停通过。
- 保留依赖弃用及 Vite 大文件提示；未运行 Docker 容器、浏览器视觉验收或大规模查询执行计划基准。新增索引的实际收益将在下一步量化，游标不提供状态快照。

## 索引任务历史（步骤 46，2026-09-15）

- 原生 PostgreSQL 17.11 / pgvector 0.8.6 全量 166 passed、0 skipped，133.07 秒；临时实例与凭据已清理。后端定向 5 passed。
- 新接口回归覆盖 20 条以外历史、先筛选再分页、下一页边界、缺失错误码、冲突条件、参数校验、鉴权和跨企业隔离。
- 前端 31 passed，覆盖服务端翻页、筛选回首页、模型参数编码、取消过期请求和失败提示；TypeScript/Vite、Ruff、Git 空白检查通过。
- 清理 build 后重新构建并隔离安装 wheel：0014、资源字节一致、上传解析、混合检索、引用问答、配额和 Worker 启停通过。
- 保留两项后端依赖弃用警告及 Vite 大文件提示。未运行 Docker 容器、浏览器视觉验收或大规模查询基准；实时 offset 分页不提供跨页快照一致性。

## 索引失败诊断（步骤 45，2026-09-15）

- 前端全量 31 passed，覆盖按原因筛选、刷新后无结果、未知/空错误码及特殊属性名兼容；原有提交确认与任务进度测试通过。
- TypeScript/Vite 生产构建、Ruff 和 Git 空白检查通过；Vite 保留既有大于 500 kB 的分包提示。
- 清理 build 后重新构建 wheel，隔离安装验证 0014、页面资源字节一致、上传解析、混合检索、引用问答、配额及 Worker 单次启停通过。
- 本轮未修改后端或迁移，未重跑 PostgreSQL 全量回归；未执行 Docker 容器或浏览器视觉验收。筛选是最近 20 条版本任务内的本地筛选，不代表完整历史。

## Worker 心跳与配置一致性（步骤 44，2026-09-15）

- 临时原生 PostgreSQL 17.11 / pgvector 0.8.6 全量回归：165 passed，0 skipped，134.90 秒；实例停止并删除生成的凭据文件。
- 新测试覆盖 30 秒边界、未来时间、策略差异、旧记录清理、Worker 模型等待期间上报、上报失败退出清理和 0013 显式升级。
- 前端全量 28 passed；TypeScript/Vite 生产构建通过，保留大于 500 kB 的既有分包提示。Ruff 通过。
- 清理构建目录后重新构建 wheel；隔离安装验证 0014、页面资源字节一致、上传解析、混合检索、引用问答、配额及实际索引 Worker 单次启停通过。
- 后端保留两项依赖弃用警告。本轮未运行 Docker 容器、浏览器视觉验收或真实大模型质量评测；心跳不构成任务进展或长期运行保证。

## 调度状态与等待指标（2026-09-15）

- 独立 wheel 重新构建并验证通过：最新前端资源、0013 升级和上传/解析/混合检索/引用问答/任务配额链路。
- 后端接口/调度/配额相关 **8 passed**，覆盖租户隔离、空队列、过期与有效租约、终止任务排除和鉴权。
- 前端全量 **27 passed**（12 文件），新增 API 策略、处理中子集、null 与 0 秒区分及空队列展示。TypeScript/Vite、Ruff、Git 空白检查通过。
- 本步无新迁移，保持 0013。未重复 PostgreSQL 全量、Docker 容器或浏览器视觉验证；构建保留既有 Ant Design 指令及大资源包提示。

## 可配置租户轮转（2026-09-15）

- 临时 PostgreSQL 17.11 + pgvector 0.8.6 全量 **160 passed、0 skipped**；调度/任务定向 **8 passed**。包含 23 表迁移/备份恢复、默认 FIFO、持久游标、并发去重与领取失败事务回滚。
- 两策略各 1/4 Worker 的 30 周期不均衡实测共 243 项任务全部成功。结果包含任务合并影响，不视为严格等工作量性能对照；临时数据库、实例、凭据已清理。
- Ruff、Git 空白检查、Compose config、独立 wheel 0013 升级及上传/解析/检索/引用问答/任务配额链路通过。Compose 提示本机 Docker 配置文件不可读，但解析成功；未运行容器。前端未改动，未重复前端测试。

## 不均衡持续入队（2026-09-14）

- 临时 PostgreSQL 17.11 + pgvector 0.8.6：高频 4 版本与三低频租户同时入队，1/4 Worker 各 30 周期；检查任务全成功、单次领取、无检查点及来源隔离。
- 原均衡模式 1/4 Worker 各一轮兼容检查通过；最终修正生产结束竞态后重新运行持续模式，结果保存。Ruff 通过，所有临时实例和凭据清理。
- 5 ms 模拟模型、短时异步 Worker，不是长期负载或多进程调度保证。本次只修改基准工具，未重复业务全量及前端测试。

## 同库多租户队列（2026-09-14）

- PostgreSQL 17.11 + pgvector 0.8.6，4 租户 × 2 版本，1/4 Worker 各 10 轮，共 160 项任务成功、attempts=1、恰好领取一次。检查点清空，所有版本原生检索来源正确，重复提交活跃任务复用 ID。
- 修正基准任务 ID 集合计数断言后重新实测通过，保存原始 JSON。Ruff、Git 空白检查通过。临时数据库、实例和凭据已清理。
- 本步只增加基准工具和文档，未重复业务全量/前端/安装包测试。模型为 5 ms 异步模拟，轮次间等待清空；不代表长时间、不均衡或多进程队列公平性。

## 多管线进程树（2026-09-14）

- 临时 PostgreSQL 17.11 + pgvector 0.8.6：2/4 个独立 128 分片完整管线进程全部成功。修正 Windows Python 启动器分组后重新实测并保存结果。
- 进程树闭包、本机 RSS、数据库异常清理共 4 项定向测试通过；Ruff 通过。没有重复业务全量或前端测试。
- Windows 实测，每组有 2 次退出/不可读采样，记录在 JSON；RSS 和工作集包含共享页，不是独占物理内存。未执行同库任务竞争、长期负载或真实模型质量测试。临时实例、数据库及凭据已清理。

## PostgreSQL 完整管线（2026-09-14）

- 临时 PostgreSQL 17.11 + pgvector 0.8.6 四场景实跑通过：16/64/128 分片构建、恢复、四类检索问答、并发配额；129 分片拦截。仅 HTTP 模拟模型。
- 并发准入每组一个 200、三个 429、一次新增 embedding。Windows Python 工作集采样成功；不是整套系统内存或并发吞吐量。
- SQLite 16 分片兼容验证通过；数据库创建失败/场景异常清理测试 2 passed，Ruff 通过。未重复业务全量、安装包或前端测试。本次首次启动审核超时，按允许规则重试后完成；临时数据库、实例与凭据均已清理。

## pgvector 并发基准（2026-09-14）

- 临时 PostgreSQL 17.11 + pgvector 0.8.6 实跑 128/1024 行 × 1/2/4 线程六组，所有参考比较通过，记录 Python 分配峰值和每线程延迟。临时实例停止、生成凭据删除。
- Ruff 通过。只增加基准工具和文档，未重复业务全量回归。不是 PostgreSQL 完整 API、并发租户、真实模型或进程 RSS 验证，详见 VECTOR_CONCURRENCY.md。

## 软件管线基准（2026-09-14）

- 执行 pipeline_benchmark 的 16/64/128/129 四个场景，全部断言通过。包含真实解析子进程、API/索引 Worker、取消后恢复、任务终态、四类检索问答和限额拦截；模型为 HTTP 模拟，数据库为临时 SQLite。
- 保存完整 JSON 采样。构建/恢复仅各测一次，每类查询采样 5 次；未执行 PostgreSQL 并发、真实模型质量、PDF/OCR 或内存峰值测试。
- Ruff 与独立 wheel 验证通过：安装后成功运行 16 分片基准，并复核 0012 升级、上传、解析、混合检索、引用问答及静态资源。未修改业务代码和前端，未重复全量回归。

## 管理端索引进度（2026-09-14）

- 独立 wheel 重新构建并验证通过：包含最新前端资源，0012 升级、上传、解析、混合检索、引用问答、任务配额链路正常。
- 后端进度接口、任务和检查点定向 **10 passed**；前端全量 **26 passed**（11 个测试文件）。覆盖批次数、租约过期/重新领取、终止清理、错误归属 404、未认证拒绝及页面状态切换。
- Ruff、TypeScript/Vite 和 Git 空白检查通过。构建仍有既有 Ant Design 指令及资源包大小提示；未做浏览器视觉、Docker 容器或 PostgreSQL 全量重复验证。数据库保持 0012。

## 内容修订与独立批次（2026-09-14）

- 临时 PostgreSQL 17.11 + pgvector 0.8.6 全量 **151 passed、0 skipped**；包含 22 表迁移/恢复、原生检索、已有中断恢复及新增批次回归。
- 定向旧链路 24 passed，新增 4 passed：修订递增/幂等/回滚、批次追加不重写旧行、连续性拒绝及清理、多批次仅两次全文快照读取、0011 → 0012 旧进度清理。新增场景使用 SQLite 与 HTTP 模拟模型，未做修订竞争压力测试。
- Ruff、Git 空白检查和独立 wheel 0012 升级、上传/解析/混合检索/引用问答、任务配额及静态资源验证通过。
- 临时数据库已停止，生成凭据已删除；保留两条已有依赖弃用提示。未执行 Docker 容器、真实模型质量或大容量端到端性能测试，128 分片上限保持。

## 索引批次检查点（2026-09-14）

- 临时 PostgreSQL 17.11 + pgvector 0.8.6 全量 **147 passed、0 skipped**，含迁移、20 表备份恢复、原生检索及既有租户隔离回归。新检查点/队列/SQLite 迁移定向 **20 passed**。
- 新增模拟进程取消后的持久恢复测试：复用首批、内容或模型变化后重建、旧 Worker 被拒绝、成功及失败清理。新检查点场景使用 SQLite 和 HTTP 模拟模型；未单独做 PostgreSQL 检查点并发压力测试。
- Ruff、Git 空白检查及独立 wheel 验证通过：0011 升级、上传、解析、混合检索、引用问答、任务与配额链路、静态资源检查。
- 临时 PostgreSQL 已停止、生成凭据已删除。未进行真实模型质量、Docker 容器或大容量端到端性能验证；保留两条既有测试依赖弃用提示。

## 分批构建失效检查（2026-09-14）

- 向量、元数据与新增批次测试：10 passed；任务、引用问答与企业模型配额回归：16 passed。Ruff 通过。
- 新增真实上传/解析产生多个批次的场景，验证成功构建以及首批期间撤回文档、撤销授权、变更模型后只调用首批，旧索引与元数据保持不变。模型服务使用 HTTP 模拟。
- 指纹与旧 JSON 编码摘要一致，包括中文、转义及嵌套值。未进行性能基准、PostgreSQL 全量或安装包重复验证；本步无数据库结构、依赖或前端变化。

## 索引元数据拆分（2026-09-14）

- 重新构建并在隔离环境验证 wheel：0010 升级、上传、解析、混合检索、引用问答、任务与配额链路及静态资源检查通过。
- 临时 PostgreSQL 17.11 + pgvector 0.8.6 全量回归 **138 passed、0 skipped**；补充已有快照的 PostgreSQL 0009 → 0010 回填后，原生定向测试 **4 passed**。
- SQLite 元数据/向量定向 **6 passed**；覆盖回填值、旧数据兼容以及状态查询和原生检索不读取完整向量 JSON。
- Ruff 通过；临时 PostgreSQL 已停止，生成凭据已删除。前端未修改，未执行真实模型质量、Docker 容器或大容量端到端验证。

## pgvector 原生验证（2026-09-11）

- 独立 wheel 验证基准模块可导入，默认上传/解析/检索/问答链路与打包资源检查通过。

- Windows 下编译官方 pgvector 0.8.6，使用临时 PostgreSQL 17.11：全量 **135 passed、0 skipped**；新增完整 API 集成后定向 **4 passed**。验证排序/隔离/重建和原生 API 分片一致性、企业隔离与撤回。
- 新增固定随机种子的原生缓存基准与原始 JSON，128/1024 行的 Top-10 重合率均 100%，详见 PGVECTOR.md。临时实例停止，凭据删除。
- Ruff 通过。前端未改动；未做真实模型质量、端到端大容量、HNSW 或 Docker 容器验证。

## pgvector 可选后端基础（2026-09-11）

- 独立 wheel 默认后端上传/解析/索引/问答回归及资源检查通过，不代表原生 pgvector 已验证。

- PostgreSQL 默认后端 **134 passed、1 skipped**；临时实例停止，凭据删除。新增默认选择/SQLite 拒绝/快照签名检查通过。
- 原生 pgvector 集成测试需 NEXUS_TEST_PGVECTOR_URL；本轮未配置，Docker 引擎未运行，因此未执行原生排序、隔离与重建测试。
- Ruff、三份 Compose 合并配置检查通过；前端未改动。仍为 128 分片精确检索，没有 HNSW 或容量性能结论。

## 企业模型调用限额与 0009（2026-09-11）

- PostgreSQL **132 passed**、前端 **24 passed**；覆盖覆盖值、零额度、继承、保留用量、企业隔离、权限/参数和 0008 显式升级。18 表恢复通过，临时实例停止、凭据删除。
- Ruff、TypeScript/Vite 与独立 wheel 0009、配置覆盖和前端资源一致性通过。未验证真实费用、Docker 实际运行或浏览器视觉。

## 当日模型用量汇总（2026-09-11）

- PostgreSQL **130 passed**、前端 **23 passed**；新增分组状态、部分/全部未知 token、企业隔离与跨日过滤测试。临时数据库停止，凭据删除。
- Ruff、TypeScript/Vite、独立 wheel 通过；依赖下载重试后完成。数据库保持 0008，无真实费用或浏览器视觉验证。

## 企业每日模型调用准入（2026-09-11）

- PostgreSQL **129 passed**、前端 **23 passed**；新增并发准入、pending 计数、企业隔离、UTC 跨日及超额不调用上游测试。临时数据库已停止、凭据删除。
- Ruff、TypeScript/Vite、Compose 配置和独立 wheel 通过。数据库保持 0008；模型为模拟上游，未验证真实费用、Docker 运行或浏览器视觉。

## 模型调用账本与 0008（2026-09-11）

- PostgreSQL 全量 **127 passed**、前端 **23 passed**；覆盖 token 已知/未知、失败记录、企业隔离、管理员分页、pending 保留、0007 显式升级和 17 表备份恢复。临时数据库停止，凭据删除。
- Ruff、TypeScript/Vite、独立 wheel 0008、模型调用记录及最终前端资源一致性通过；2 项已有依赖弃用提示。
- 模型为模拟上游，未验证真实费用对账、浏览器视觉、Docker 容器或压力测试。账本记录调用结果，不保证业务成功或上游账单精度。

## 企业差异化限额与 0007（2026-09-11）

- 独立 wheel 验证 0007、上传/解析/检索/问答、后台索引消费、企业限额设零后拒绝新任务及前端资源字节一致性通过。

- 临时 PostgreSQL 全量 **124 passed**、前端 **22 passed**。覆盖覆盖值/继承/零额度、企业隔离、权限与严格参数校验、审计事件、0006 显式升级；16 表备份恢复与受限角色通过。临时实例停止，凭据删除。
- Ruff、TypeScript/Vite 通过；模型使用模拟上游，未进行真实模型质量、浏览器视觉或 Docker 实际启动验证。

## 统一索引准入与每日任务额度（2026-09-10）

- 临时 PostgreSQL 全量 **122 passed**，前端 **21 passed**；配额定向 **4 passed**，覆盖同步/后台共享额度、重复复用、失败计数、跨日重置、活跃冲突及并发准入。临时实例停止，凭据删除。
- TypeScript/Vite、Ruff、Compose 配置与独立 wheel 索引/后台消费链路通过。数据库保持 0006。
- 尚未运行真实模型费用计量、Docker 容器或浏览器视觉验证。当前限额按任务创建数量计算，不是 token、金额或全部模型请求的配额。

## 后台索引任务与 0006（2026-09-10）

- PostgreSQL 17.11 完整 **118 passed**，包含 0005 显式升级、任务去重、旧租约禁止发布、3 次中断失败、授权撤销、队列容量、15 表备份恢复与受限运行角色检查。临时数据库已停止，凭据已删除；2 项既有依赖弃用提示。
- 前端 **21 passed**，索引操作改为 POST index-jobs；TypeScript/Vite、Ruff 通过。
- Compose PostgreSQL 合并配置检查通过；独立 wheel 验证 0006、上传/解析、检索/问答、后台任务提交消费及前端资源一致性。
- 模型为模拟上游，未运行真实模型质量、Docker 容器、浏览器视觉或高并发压力测试。队列活跃数限制不等同于企业总用量配额。

## 评测基础与 CSS 类型（2026-09-10）

- `vite-env.d.ts` 引入 Vite 客户端类型；TypeScript/Vite 构建成功，保留既有依赖与包体积提示。
- 新增评测器测试，验证来源命中、拒答计分以及报告不含原文或生成回答；Ruff 通过。
- 初始 5 题基于项目实际文档，尚未执行真实企业模型评测，不声称回答准确率；未修改数据库与 Worker。

## 模型索引页面、混合检索与引用问答（2026-09-10）

- PostgreSQL 17.11 完整回归 **111 passed**；随后新增两个边界案例，问答定向 **10 passed**，覆盖 RRF 去重、企业隔离、引用伪造/正文不匹配拒绝、无引用拒答、生成中撤销聊天授权或撤回文档、缺少索引不调用聊天模型。临时数据库已停止，凭据已删除。
- 前端 **21 passed**，覆盖授权模型筛选、确认后建立索引、模型切换取消请求、独立聊天模型参数与引用纯文本渲染；TypeScript/Vite、Ruff 通过。构建保留既有 Ant Design 指令和包体积提示。
- 独立 wheel 环境验证 0005 迁移、上传、隔离解析、发布、向量建立、混合检索、引用问答和打包前端资源字节一致性。
- 模型为 HTTP 模拟上游；未进行真实模型检索/引用质量、浏览器视觉、Docker 实际启动或负载验证。引用编号校验不等于语义证据校验。

## 向量索引与 0005（2026-09-10）

- PostgreSQL 17.11 全量 **103 passed、0 skipped**，2 项已有弃用警告。覆盖向量快照持久化和 SQLite 导入、企业/模型授权、零向量拒绝、配置变化和撤回失效、上游等待期间撤回、建立期间配置变化、容量拒绝、失败不覆盖旧快照、0004 显式升级及原模型网关回归。
- 14 表备份恢复摘要一致，受限数据库账号下个人企业成员查询恢复向量快照通过；临时 PostgreSQL 停止，生成凭据删除。embedding 使用 HTTP 模拟服务。
- Ruff 通过；重新构建 wheel，在临时独立环境执行 0005 升级、上传/解析/发布、关键词检索、向量建立/检索和页面资源一致检查通过。
- 前端未修改、未重跑前端测试；未执行真实云端/本地模型联调、向量质量基准、Docker 容器、浏览器或规模压力测试。128 分片的 JSON 快照是小规模基础，不代表 pgvector/ANN、后台大批量索引或混合检索已完成。

## 关键词检索（2026-09-10）

- PostgreSQL 17.11 全量 **99 passed、0 skipped**，2 项已有弃用警告；知识库定向 26 passed。新增验证中文/英文排序、归一化、来源字段、空结果、企业隔离、未发布/撤回/退役/停用过滤、参数与容量边界、稳定排序和 bootstrap 拒绝。
- 恢复演练中增加受限数据库角色下个人企业成员 search API 查询，返回恢复文档的正确来源 ID；临时数据库已停止，生成凭据文件已删除。
- 前端 **18 passed**（本机限制两个并发测试 Worker），TypeScript/Vite 和 Ruff 通过。新增表单使 jsdom 整表可访问性样式扫描超时，改用目标文本定位，保留原 5 秒超时及交互断言后通过。
- 新 JS 967.12 KB（gzip 306.88 KB），CSS 0.85 KB；既有第三方指令和大块提示未消除。独立 wheel 验证最新资源一致，上传→子进程解析→发布→关键词检索与页面加载通过。
- 本轮无新增迁移；未执行 Docker、实际浏览器视觉、真实 embedding/聊天服务、规模压力或检索质量基准测试。关键词基线不代表语义检索和引用问答已完成。

## 知识库管理页面（2026-09-10）

- 前端 **16 passed**：涵盖文件类型校验与上传、版本路径接入、发布确认、纯文本分片、失败重试、服务端分页、卸载取消、原始二进制请求、文件名编码和过期会话清理。原企业与个人身份测试通过。
- TypeScript 与 Vite 构建通过；修正新增测试的 mock 参数类型后重新执行测试/构建。产物 JS 965.29 KB（gzip 306.41 KB）、CSS 0.85 KB；既有 Ant Design use client 和大块提示仍在。
- 后端页面测试 1 passed、Ruff 通过。重新构建 wheel，在临时独立虚拟环境验证最新前端资源与构建目录逐字节一致、页面加载、0004、上传及真实解析子进程 ready 状态通过。
- Browser 连接后可用浏览器列表为空，未进行真实浏览器视觉/文件选择器交互。未修改数据库和 Worker，未重跑 PostgreSQL 全量、Docker 容器或真实模型。上一轮 95 项后端测试属于上一阶段结果。

## 知识库入库与 0004（2026-09-09）

- 最终 PostgreSQL 17.11 全量 **95 passed、0 skipped**，2 项已有依赖弃用警告。新增 7 项入库测试：上传/去重/错误输入、发布撤回与跨企业隔离、子进程解析、失败重试、租约恢复与过期凭证、并发领取、原文件与分片导入、分片写入回滚、bootstrap 范围拒绝和 0003 升级。
- pg_dump/pg_restore 恢复验证扩展至 13 表；原文件及分片摘要一致，受限角色下个人企业会话读取文档分片通过。临时 PostgreSQL 已停止，生成密码文件已删除。
- Ruff 通过；SQLite 与 PostgreSQL 两套 Compose config --quiet 通过。Docker 引擎未运行，未执行容器构建/启动，也未实测 POSIX 的资源硬限制。
- 清理 build 后构建 wheel；临时独立虚拟环境安装哈希锁依赖及 wheel 内容，验证 0004、上传 API、真实解析子进程、ready 持久化和页面资源通过。初次临时环境缺少依赖，补充安装后通过。
- 前端未修改，未重复浏览器/前端测试。当前没有上传管理页面；没有真实模型联调、OCR、向量检索、队列压力测试或生产配额验证。原生 Windows 仅有解析超时及大小限制，没有 POSIX 地址空间硬限制。

## 文档解析与来源分片（2026-09-09）

- 后端全量：86 passed、2 skipped（未配置 PostgreSQL）、2 项现有弃用警告；新增 15 项覆盖中文偏移与内容重建、DOCX 表格段落、真实生成 PDF 文本/页码/空白页、加密拒绝、无效格式、大小限制、XML 实体拒绝和 CLI JSON/退出码。
- Ruff 通过；新增依赖哈希锁安装通过；清理 build 后重新构建 wheel，隔离源码路径验证解析模块、CLI 文件、命令注册和文本预览通过。
- 本轮不涉及数据库结构或前端，没有重跑 PostgreSQL 实库、前端构建/交互、Docker 容器和真实模型。未完成不可信文件的进程资源隔离测试，不能据此开放公网上传。

## 本轮应用版本与 0003（2026-09-09）

- 常规回归 71 passed、2 skipped；启动专用临时 PostgreSQL 17.11 后全量 **73 passed、0 skipped**，2 项既有依赖警告。
- 验证企业应用隔离、重复标识/版本、草稿隐藏、发布/退役约束、禁用应用、错误归属、bootstrap 范围拒绝、并发发布幂等、事务审计、0002 升级和应用数据导入。
- 真实 pg_dump/pg_restore 校验 11 表摘要；恢复后用个人企业成员会话读取已发布版本，并检查受限运行角色和新增应用事件序列。
- 前端 **9 项通过**，新增发布确认及企业路径测试；修正初次选择企业时重置未挂载表单的警告。TypeScript、Vite、Ruff 通过，构建仍有约 958 KB 大块提示（gzip 约 304 KB）。
- 最终 wheel 独立验证包含 0003、应用创建、版本发布、个人登录退出、页面与哈希 JS/CSS 均通过。
- 临时数据库已停止，生成凭据文件已删除。Docker 容器运行、真实模型、浏览器视觉交互和远端 CI 未执行。

## 本轮个人身份与 0002（2026-09-09）

- SQLite 全量：67 passed、2 skipped；随后启动独立 PostgreSQL 17.11，全量 **69 passed、0 skipped**，保留 2 项既有依赖弃用警告。
- 覆盖密码/会话摘要、重新登录撤销、退出、到期、账号停用/重置、5 次失败锁定及恢复、角色校验、企业越权拒绝、个人模型审计、0001 显式升级及身份数据导入。
- PostgreSQL 恢复增加个人账号、密码摘要和会话内容校验，八表摘要一致；受限运行角色可读取身份表，已有恢复后 API 及序列验证通过。
- Vitest **8 项通过**，包括个人登录/退出、企业成员后台拒绝、到期回调以及原企业页面用例；TypeScript、Vite 和 Ruff 通过。大块构建提示仍存在（gzip 约 303 KB）。
- 最终账号列表 enabled 布尔类型校正后，个人账号生命周期定向测试再次通过。
- wheel 独立解包验证包含 0002，SQLite 迁移、个人账号创建/登录/管理/退出，以及企业页和 JS/CSS 正常。
- 临时 PostgreSQL 仅监听本机随机端口，结束已停止，生成的凭据文件已删除；未创建系统服务。真实模型、Docker 实际运行、浏览器视觉交互和远端 CI 本轮未执行。

## 本轮 React 企业页面（2026-09-09）

- 最终 wheel 独立解包验证通过：SQLite 迁移、check、健康、原模型页面、企业页及其哈希 JS/CSS、模型列表均正常。首次验证进程未返回，停止后增加堆栈诊断与超时重试通过；最终包发现配置下重建复验也通过。
- Python：60 passed、2 skipped、2 项既有警告；PostgreSQL 未启动，实库测试跳过。
- Vitest：5 项通过，覆盖创建/凭据清除、撤销空响应、轮换确认、断开后的迟到响应和错误请求 ID。
- 测试发现 Ant Design 隐藏弹窗会保留凭据内容，已改为关闭立即卸载并验证 DOM 清除。
- TypeScript、Vite 生产构建与 Ruff 通过；构建包含第三方 use client 提示及约 948 KB 大块提示（gzip 约 301 KB），后续拆分加载。
- 页面 HTTP 测试验证未构建返回 503、构建后加载、企业页样式策略和管理接口鉴权。
- Docker 配置已更新，容器构建/运行、真实模型、真实浏览器视觉交互和远端 CI 本轮未执行。

## 本轮顶层分层验证（2026-09-09）

- pytest：59 passed、2 skipped、2 项既有弃用警告；本轮未启动 PostgreSQL，不将历史实库结果计入本轮。
- Ruff 检查 api/cli/web、前端 JS 语法、新旧 CLI 帮助入口通过。
- 重新安装可编辑包生成新源码映射；干净 wheel 包含 API、CLI 和 web 资源。
- 独立解包运行 SQLite 迁移、check、健康、管理页面及 JS/CSS、模型列表均通过。
- 根目录指定 --project-directory .，SQLite 和 PostgreSQL 两套 Compose config --quiet 通过；客户端提示用户 Docker config 无读取权限，未影响编排解析。
- 容器运行、真实模型、浏览器视觉交互和远端 CI 未验证。

## 本轮目录重构（2026-09-09）

- pytest：59 passed、2 skipped、2 项既有弃用警告；本轮未启动 PostgreSQL，实库与恢复测试跳过，上一轮 61 项通过属于历史记录。
- Ruff 与迁移后的 admin.js 语法检查通过。
- wheel 构建通过；首次构建发现 build 中的旧模块残留，清理该生成目录后重建并确认无旧目录。
- 从 wheel 解包路径独立导入，验证 SQLite Alembic 升级、数据库 check、健康接口、管理页面/JS/CSS 及模型列表通过。
- HTTP URL、启动与数据库 CLI 入口保持原约定；内部导入及资源位置映射见 architecture/CODE_MAP.md。
- Docker 实际启动、真实模型及浏览器视觉交互本轮未验证，未执行远端 CI。

## PostgreSQL 实库及备份恢复完成（2026-09-09）

- 环境：Windows、Python 3.12.14、独立 PostgreSQL 17.11，仅监听本机 55439。
- 完整测试：61 passed，0 skipped，仍有两条既有测试依赖弃用提示；Ruff 通过。
- 真实验证迁移、SQLite 导入、企业认证/授权、并发写入、pg_dump/pg_restore 恢复与五表摘要一致性、恢复后的 API 和审计序列。
- 运行角色权限验证通过：所需 DML 正常，DDL 和迁移版本修改被拒绝。
- 环境问题：原目录特殊字符导致 initdb 失败，改用 ASCII 临时目录解决；沙箱历史测试目录权限冲突通过本次专用目录解决。最终结果无失败或跳过。
- 已停止测试实例，确认残留演练数据库/角色均为 0，生成的凭据文件已移除。
- 真实模型、Docker 实际运行及生产灾备目标不在本次已验证范围。复现流程见 BACKUP_RESTORE.md。

## 健康检查与诊断优化（2026-09-09）

- 59 项通过，1 项 PostgreSQL 实库测试跳过；Ruff 通过。
- 覆盖缺表检测及恢复、未知版本拒绝、存活/就绪区分、检查不解析模型记录、不创建缺失文件、不修改已存在文件，以及数据库错误脱敏。
- PostgreSQL 连接池与锁超时配置尚未实库验证；保留既有两条测试依赖弃用提示。

## 双数据库存储与迁移验证（2026-09-08）

- 55 项测试通过、1 项 PostgreSQL 集成测试跳过：本机无 PostgreSQL 服务，Docker Engine 未运行。
- 已验证原有 48 项回归、旧/新 SQLite 迁移、导入摘要一致性、凭据与授权保留、失败回滚、非空及同源拒绝、特殊字符文件路径、配置 URL 优先级。
- Ruff、JavaScript 语法和 PostgreSQL Compose 合并配置检查通过。Docker 客户端的配置文件权限警告仍存在，但不影响静态配置解析。
- wheel 构建成功，包含 migrations/env.py 与 versions/0001_storage.py。
- CI 已配置独立 PostgreSQL 17 服务和随机测试 schema，但未推送远端，因此 CI 本轮尚未执行。
- 实库锁行为、自增序列恢复和端到端 PostgreSQL 路径需由上述集成测试实测；生产备份恢复演练也仍待完成。

## 企业模型接入验证（2026-09-08，0.3）

- 48 项测试通过，含企业模型列表隔离、对话与向量越权拒绝、伪造企业字段、全局/管理员令牌隔离、停用/撤权/轮换和重启。
- 验证密钥明文未进入数据库、列表和事件响应；重复授权不重复记事件，无效目标不授权。
- Ruff、JavaScript 语法与 Compose tenant 模式配置检查通过。Docker 客户端仍提示用户配置文件无读取权限，配置解析成功，不代表容器启动成功。
- 仍有两条既有测试依赖弃用提示，无新增测试失败。
- 未执行真实模型联调、容器运行和浏览器交互；企业接入 API 通过 TestClient 验证。

## 配置并发保护验证（2026-09-08）

- 40 项 Python 测试通过，Ruff 和 JavaScript 语法检查通过。
- 两个线程携带相同快照写入不同配置：一个成功、一个冲突，审计只有成功写入的事件。
- 旧快照、重复创建、缺少或非法条件头按约定拒绝；相同配置重复保存不新增审计。
- 创建响应与重新读取的 ETag 一致，数值默认值经过规范化后计算摘要。
- 本次浏览器交互、真实模型和 Docker 启动未实测；只验证对应 API、事务及脚本语法。

## 配置审计增量验证（2026-09-08）

- 36 项 Python 测试通过；新增审计字段、重复保存、权限、分页、重启保留和事务回滚验证。
- Ruff、JavaScript 语法及 Git 空白检查通过。
- 工作台新增审计列表、筛选和分页，浏览器视觉及实际交互未实测。
- 当前日志身份仍为共享管理员类别，个人身份追踪依赖后续用户体系。

## 0.2 增量验证（2026-09-08）

- Python 自动化测试：32 项通过，包含原 21 项回归。
- 新增覆盖：管理员试调用、向量试调用、权限/禁用/输入拒绝、兼容 token 映射、temperature 能力、嵌套畸形响应、8 MiB 限制、慢流总时限及连接关闭、静态页面资源、CSP 和配置接口鉴权、旧配置默认值。
- Ruff 静态检查通过；`node --check web/src/agent_nexus_web/static/admin.js` 通过。
- 0.2.0 wheel 安装包构建成功，确认包含 index.html、admin.css 和 admin.js，可随 Python/Docker 部署交付。
- Browser 运行时未找到可用浏览器，因此页面视觉和实际点击流程尚未验证。API 和静态资源通过 TestClient 验证，不等价于浏览器交互验证。
- 未提供真实模型服务，未进行真实供应商联调；Docker 实际启动仍未验证。

以下为 0.1 的历史记录。

日期：2026-09-08。环境：Windows，独立 Python 3.12.14。

## 已完成

- `pytest -q`：21 项测试通过。
- `ruff check --config pyproject.toml api cli web`：通过。
- `docker compose config --quiet`：通过；本机 Docker 客户端提示用户配置文件无读取权限，未影响配置解析。
- `git diff --check`：通过。
- 运行依赖已锁定版本及文件哈希。

测试覆盖兼容接口/Ollama 的请求与响应转换、向量顺序、数量与维度、调用/管理员权限分离、模型禁用及能力拒绝、端点允许列表、凭据引用及持久化、超时/限流/异常响应脱敏。

两条测试依赖弃用提示来自 Starlette 对 httpx 测试客户端与 AnyIO 别名的使用，当前不影响测试结果；后续随依赖升级处理。

## 尚未验证

- Docker 镜像构建及容器实际启动：本机 Docker Engine 未运行。已提供 CI 构建步骤，但尚未在远端执行。
- 真实云端模型调用：未提供可用端点和凭据。
- 本地 Ollama/vLLM 联调：未发现已配置的模型服务，本轮未安装模型。
- Linux 原生进程运行及性能压测。

模拟协议测试不代表所有供应商、模型和参数组合均已兼容。接入时先注册真实模型，分别调用对话/向量接口进行能力验收。
