# 第一阶段验证记录

## 文档解析与来源分片（2026-09-09）

- 后端全量：86 passed、2 skipped（未配置 PostgreSQL）、2 项现有弃用警告；新增 15 项覆盖中文偏移与内容重建、DOCX 表格段落、真实生成 PDF 文本/页码/空白页、加密拒绝、无效格式、大小限制、XML 实体拒绝和 CLI JSON/退出码。
- Ruff 通过；新增依赖哈希锁安装通过；清理 build 后重新构建 wheel，隔离源码路径验证解析模块、CLI 文件、命令注册和文本预览通过。
- 本轮不涉及数据库结构或前端，没有重跑 PostgreSQL 实库、前端构建/交互、Docker 容器和真实模型。未完成不可信文件的进程资源隔离测试，不能据此开放公网上传。

## 本轮应用版本与 0003（2026-09-09）

- 常规回归 71 passed、2 skipped；启动专用临时 PostgreSQL 17.11 后全量 **73 passed、0 skipped**，2 项既有依赖警告。
- 验证企业应用隔离、重复标识/版本、草稿隐藏、发布/退役约束、禁用应用、错误归属、bootstrap 范围拒绝、并发发布幂等、事务审计、0002 升级和应用数据导入。
- 真实 pg_dump/pg_restore 校验 11 表摘要；恢复后用个人企业成员会话读取已发布版本，并检查受限运行角色和新增应用事件序列。
- 前端 **9 项通过**，新增发布确认及企业路径测试；修正初次选择企业时重置未挂载表单的警告。TypeScript、Vite、Ruff 通过，构建仍有约 958 KB 大块提示（gzip 约 304 KB）。
- 最终 wheel 独立验证包含 0003、应用创建、版本发布、个人登录退出、页面与哈希 JS/CSS 均通过。
- 临时数据库已停止，生成凭据文件已删除。Docker 容器运行、真实模型、浏览器视觉交互和远端 CI 未执行。

## 本轮个人身份与 0002（2026-09-09）

- SQLite 全量：67 passed、2 skipped；随后启动独立 PostgreSQL 17.11，全量 **69 passed、0 skipped**，保留 2 项既有依赖弃用警告。
- 覆盖密码/会话摘要、重新登录撤销、退出、到期、账号停用/重置、5 次失败锁定及恢复、角色校验、企业越权拒绝、个人模型审计、0001 显式升级及身份数据导入。
- PostgreSQL 恢复增加个人账号、密码摘要和会话内容校验，八表摘要一致；受限运行角色可读取身份表，已有恢复后 API 及序列验证通过。
- Vitest **8 项通过**，包括个人登录/退出、企业成员后台拒绝、到期回调以及原企业页面用例；TypeScript、Vite 和 Ruff 通过。大块构建提示仍存在（gzip 约 303 KB）。
- 最终账号列表 enabled 布尔类型校正后，个人账号生命周期定向测试再次通过。
- wheel 独立解包验证包含 0002，SQLite 迁移、个人账号创建/登录/管理/退出，以及企业页和 JS/CSS 正常。
- 临时 PostgreSQL 仅监听本机随机端口，结束已停止，生成的凭据文件已删除；未创建系统服务。真实模型、Docker 实际运行、浏览器视觉交互和远端 CI 本轮未执行。

## 本轮 React 企业页面（2026-09-09）

- 最终 wheel 独立解包验证通过：SQLite 迁移、check、健康、原模型页面、企业页及其哈希 JS/CSS、模型列表均正常。首次验证进程未返回，停止后增加堆栈诊断与超时重试通过；最终包发现配置下重建复验也通过。
- Python：60 passed、2 skipped、2 项既有警告；PostgreSQL 未启动，实库测试跳过。
- Vitest：5 项通过，覆盖创建/凭据清除、撤销空响应、轮换确认、断开后的迟到响应和错误请求 ID。
- 测试发现 Ant Design 隐藏弹窗会保留凭据内容，已改为关闭立即卸载并验证 DOM 清除。
- TypeScript、Vite 生产构建与 Ruff 通过；构建包含第三方 use client 提示及约 948 KB 大块提示（gzip 约 301 KB），后续拆分加载。
- 页面 HTTP 测试验证未构建返回 503、构建后加载、企业页样式策略和管理接口鉴权。
- Docker 配置已更新，容器构建/运行、真实模型、真实浏览器视觉交互和远端 CI 本轮未执行。

## 本轮顶层分层验证（2026-09-09）

- pytest：59 passed、2 skipped、2 项既有弃用警告；本轮未启动 PostgreSQL，不将历史实库结果计入本轮。
- Ruff 检查 api/cli/web、前端 JS 语法、新旧 CLI 帮助入口通过。
- 重新安装可编辑包生成新源码映射；干净 wheel 包含 API、CLI 和 web 资源。
- 独立解包运行 SQLite 迁移、check、健康、管理页面及 JS/CSS、模型列表均通过。
- 根目录指定 --project-directory .，SQLite 和 PostgreSQL 两套 Compose config --quiet 通过；客户端提示用户 Docker config 无读取权限，未影响编排解析。
- 容器运行、真实模型、浏览器视觉交互和远端 CI 未验证。

## 本轮目录重构（2026-09-09）

- pytest：59 passed、2 skipped、2 项既有弃用警告；本轮未启动 PostgreSQL，实库与恢复测试跳过，上一轮 61 项通过属于历史记录。
- Ruff 与迁移后的 admin.js 语法检查通过。
- wheel 构建通过；首次构建发现 build 中的旧模块残留，清理该生成目录后重建并确认无旧目录。
- 从 wheel 解包路径独立导入，验证 SQLite Alembic 升级、数据库 check、健康接口、管理页面/JS/CSS 及模型列表通过。
- HTTP URL、启动与数据库 CLI 入口保持原约定；内部导入及资源位置映射见 architecture/CODE_MAP.md。
- Docker 实际启动、真实模型及浏览器视觉交互本轮未验证，未执行远端 CI。

## PostgreSQL 实库及备份恢复完成（2026-09-09）

- 环境：Windows、Python 3.12.14、独立 PostgreSQL 17.11，仅监听本机 55439。
- 完整测试：61 passed，0 skipped，仍有两条既有测试依赖弃用提示；Ruff 通过。
- 真实验证迁移、SQLite 导入、企业认证/授权、并发写入、pg_dump/pg_restore 恢复与五表摘要一致性、恢复后的 API 和审计序列。
- 运行角色权限验证通过：所需 DML 正常，DDL 和迁移版本修改被拒绝。
- 环境问题：原目录特殊字符导致 initdb 失败，改用 ASCII 临时目录解决；沙箱历史测试目录权限冲突通过本次专用目录解决。最终结果无失败或跳过。
- 已停止测试实例，确认残留演练数据库/角色均为 0，生成的凭据文件已移除。
- 真实模型、Docker 实际运行及生产灾备目标不在本次已验证范围。复现流程见 BACKUP_RESTORE.md。

## 健康检查与诊断优化（2026-09-09）

- 59 项通过，1 项 PostgreSQL 实库测试跳过；Ruff 通过。
- 覆盖缺表检测及恢复、未知版本拒绝、存活/就绪区分、检查不解析模型记录、不创建缺失文件、不修改已存在文件，以及数据库错误脱敏。
- PostgreSQL 连接池与锁超时配置尚未实库验证；保留既有两条测试依赖弃用提示。

## 双数据库存储与迁移验证（2026-09-08）

- 55 项测试通过、1 项 PostgreSQL 集成测试跳过：本机无 PostgreSQL 服务，Docker Engine 未运行。
- 已验证原有 48 项回归、旧/新 SQLite 迁移、导入摘要一致性、凭据与授权保留、失败回滚、非空及同源拒绝、特殊字符文件路径、配置 URL 优先级。
- Ruff、JavaScript 语法和 PostgreSQL Compose 合并配置检查通过。Docker 客户端的配置文件权限警告仍存在，但不影响静态配置解析。
- wheel 构建成功，包含 migrations/env.py 与 versions/0001_storage.py。
- CI 已配置独立 PostgreSQL 17 服务和随机测试 schema，但未推送远端，因此 CI 本轮尚未执行。
- 实库锁行为、自增序列恢复和端到端 PostgreSQL 路径需由上述集成测试实测；生产备份恢复演练也仍待完成。

## 企业模型接入验证（2026-09-08，0.3）

- 48 项测试通过，含企业模型列表隔离、对话与向量越权拒绝、伪造企业字段、全局/管理员令牌隔离、停用/撤权/轮换和重启。
- 验证密钥明文未进入数据库、列表和事件响应；重复授权不重复记事件，无效目标不授权。
- Ruff、JavaScript 语法与 Compose tenant 模式配置检查通过。Docker 客户端仍提示用户配置文件无读取权限，配置解析成功，不代表容器启动成功。
- 仍有两条既有测试依赖弃用提示，无新增测试失败。
- 未执行真实模型联调、容器运行和浏览器交互；企业接入 API 通过 TestClient 验证。

## 配置并发保护验证（2026-09-08）

- 40 项 Python 测试通过，Ruff 和 JavaScript 语法检查通过。
- 两个线程携带相同快照写入不同配置：一个成功、一个冲突，审计只有成功写入的事件。
- 旧快照、重复创建、缺少或非法条件头按约定拒绝；相同配置重复保存不新增审计。
- 创建响应与重新读取的 ETag 一致，数值默认值经过规范化后计算摘要。
- 本次浏览器交互、真实模型和 Docker 启动未实测；只验证对应 API、事务及脚本语法。

## 配置审计增量验证（2026-09-08）

- 36 项 Python 测试通过；新增审计字段、重复保存、权限、分页、重启保留和事务回滚验证。
- Ruff、JavaScript 语法及 Git 空白检查通过。
- 工作台新增审计列表、筛选和分页，浏览器视觉及实际交互未实测。
- 当前日志身份仍为共享管理员类别，个人身份追踪依赖后续用户体系。

## 0.2 增量验证（2026-09-08）

- Python 自动化测试：32 项通过，包含原 21 项回归。
- 新增覆盖：管理员试调用、向量试调用、权限/禁用/输入拒绝、兼容 token 映射、temperature 能力、嵌套畸形响应、8 MiB 限制、慢流总时限及连接关闭、静态页面资源、CSP 和配置接口鉴权、旧配置默认值。
- Ruff 静态检查通过；`node --check web/src/agent_nexus_web/static/admin.js` 通过。
- 0.2.0 wheel 安装包构建成功，确认包含 index.html、admin.css 和 admin.js，可随 Python/Docker 部署交付。
- Browser 运行时未找到可用浏览器，因此页面视觉和实际点击流程尚未验证。API 和静态资源通过 TestClient 验证，不等价于浏览器交互验证。
- 未提供真实模型服务，未进行真实供应商联调；Docker 实际启动仍未验证。

以下为 0.1 的历史记录。

日期：2026-09-08。环境：Windows，独立 Python 3.12.14。

## 已完成

- `pytest -q`：21 项测试通过。
- `ruff check --config pyproject.toml api cli web`：通过。
- `docker compose config --quiet`：通过；本机 Docker 客户端提示用户配置文件无读取权限，未影响配置解析。
- `git diff --check`：通过。
- 运行依赖已锁定版本及文件哈希。

测试覆盖兼容接口/Ollama 的请求与响应转换、向量顺序、数量与维度、调用/管理员权限分离、模型禁用及能力拒绝、端点允许列表、凭据引用及持久化、超时/限流/异常响应脱敏。

两条测试依赖弃用提示来自 Starlette 对 httpx 测试客户端与 AnyIO 别名的使用，当前不影响测试结果；后续随依赖升级处理。

## 尚未验证

- Docker 镜像构建及容器实际启动：本机 Docker Engine 未运行。已提供 CI 构建步骤，但尚未在远端执行。
- 真实云端模型调用：未提供可用端点和凭据。
- 本地 Ollama/vLLM 联调：未发现已配置的模型服务，本轮未安装模型。
- Linux 原生进程运行及性能压测。

模拟协议测试不代表所有供应商、模型和参数组合均已兼容。接入时先注册真实模型，分别调用对话/向量接口进行能力验收。
