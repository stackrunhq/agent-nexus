# 分阶段执行方案

每阶段产出可启动、可测试的闭环，再进入下一阶段。业务模块仅通过统一模型网关调用模型。

## 01 项目基础与统一模型网关（当前已扩展到 0.2）

交付：FastAPI、模型配置管理 API、模型列表、文本对话、向量、请求 ID、统一错误、角色令牌、Docker 与原生部署配置。

适配云端兼容协议、vLLM 等本地兼容协议、Ollama 原生协议。通过别名区分部署位置和能力，密钥使用环境变量引用。

0.2 增量：内置轻量管理页面、配置编辑及启停、管理员能力试调用、耗时/用量展示、兼容接口 token 参数映射、temperature 能力拒绝、总时限和响应大小约束。旧配置自动使用兼容默认值，无需数据迁移。

当前轻量页面随 API 部署，下一阶段的企业级 React 后台仍未实现。

已追加配置变更审计 API 和工作台日志查询。代码开发过程按 CONTRIBUTING.md 分步骤更新 CHANGELOG.md 并创建本地 Git 提交，不推送远端。

已加入配置 ETag 条件写入，防止多个管理员页面互相覆盖；此能力属于进入企业后台前的配置管理加固，企业用户体系与 PostgreSQL 仍在下一阶段。

验收：切换别名调用不同协议；能力不符及禁用模型不请求上游；异常不泄露原始响应；配置重启保留；自动化协议测试通过。真实模型及 Docker 运行验证单独记录，不以模拟测试代替。

2026-09-09 目录调整：完成按业务分包、HTTP 公共处理拆分、测试归档和阅读导航；现有部署命令保留。企业页面、用户及角色仍是下一项业务增量，未因本次重构提前标记完成。

顶层分层更新：API、前端、CLI、容器配置已独立目录；React/TypeScript/Vite/Ant Design 在 web/ 中推进，Redis/Celery/S3/pgvector 尚待实现。

React 企业页面增量已完成：创建、启停、轮换凭据、模型授权、事件查看及打包链路；个人用户/角色尚待开发。

个人身份增量：0002 迁移、平台管理员/企业成员两角色、可撤销会话与账号管理已实现。下一项推进企业应用和版本，更细组织角色、自助改密及审计补全另行跟踪。

## 02 企业与后台基础

0.3 已先实现企业机器凭据、模型授权/撤销、停用、轮换和调用层授权 API，详见 TENANT_ACCESS.md。用户/角色、企业页面和文档数据隔离尚未完成。当前整体状态见 STATUS.md。

数据库增量已交付 SQLAlchemy 存储、Alembic 0001、旧 SQLite 导入、PostgreSQL Compose 与 CI 集成测试；本机无数据库服务，实际 PostgreSQL 测试尚未执行。下一步先完成实库验证和备份恢复，再推进企业页面与用户/角色。

2026-09-09 更新：独立 PostgreSQL 17.11 实库及备份恢复已完成，61 项测试全部通过；同时验证受限运行角色。下一步推进企业页面与用户/角色，生产备份策略及部署账户分离仍单独跟进。

依赖 01。交付 PostgreSQL + Alembic、企业/客户组织/用户/角色、应用及版本、管理员登录、React + TypeScript 后台、模型配置页面和试调用。

当前配置导入 PostgreSQL；增加 secret 引用服务、修改审计、企业模型可见性及额度。全局调用令牌替换为服务凭据/用户短期令牌，企业身份从可信认证上下文取得。

验收：A 企业无法列出或调用 B 企业模型及应用；用户不能管理后台；数据库和后台任务的租户隔离测试通过。完成前不接入真实多企业私有数据。

0003 应用版本基础已完成：应用创建/启停、版本草稿/发布/退役、企业读取隔离、操作记录。多格式离线解析与来源分片已完成，见 [知识库解析](KNOWLEDGE.md)；0004 已接入上传、原文件/分片持久化、独立 Worker 与文档发布读取；React 上传、处理状态、分片预览、重试及发布/撤回页面已完成；带发布权限过滤的关键词检索与来源预览已完成；0005 已实现小规模持久化向量索引 API，下一项是向量管理页面、混合检索和问答，OCR 尚未实现。

## 03 知识库与 RAG

依赖 02。交付 S3 存储、Redis/Celery、PDF/Markdown/DOCX 解析、必要的 OCR、切片来源、版本发布撤回、向量及中文关键词索引、混合检索、引用回答。

索引绑定 embedding 模型和版本，换模型先重建再切换。模型不能突破检索权限，文档指令视为不可信资料。

验收：至少 100 个真实问题，核验出处和版本；测试未知问题、越权、撤回和更新中断恢复。

## 04 用户使用指导

依赖 03。交付帮助中心、会话、SSE、断线恢复、iframe SDK、反馈、真实页面入口和截图、分步指引。

验收：小白根据手册完成业务任务；刷新后恢复步骤；浏览器不持有企业长期密钥。

## 05 可控业务流程

依赖 04。交付审核流程模板、计划及状态机、参数校验、只读/写入工具、用户确认、业务审批、幂等、回调验签、未知结果查询及补偿。

先完成一个真实应用的“查询 → 创建草稿 → 确认 → 提交 → 查询结果”。模型建议调用，后端决定授权；不接受任意 URL、SQL 或代码执行。

验收：超时及重复回调不产生重复订单；重启恢复；越权不调用业务端；等待审批不误报完成。

## 06 上线运维

依赖 02–05。交付审计、成本用量、配额限流、观测告警、CI/CD、锁定依赖和镜像、安全扫描、备份恢复、升级文档及 Kubernetes 清单。

先单企业试点再扩展。首个完整试点预算约 9–13 周（1 前端、2 后端/AI，配合产品和测试），依实际资料与接口调整。

## 后续增强

按需追加原生供应商协议、流式工具调用、图片、重排、租户授权的失败切换及可视化流程编辑。实际能力须评测，不承诺任意模型等价。

## 步骤 21：评测基础已完成

后续步骤 22 已完成持久化后台索引任务与队列容量保护，详见 [索引任务](INDEX_JOBS.md)。企业总用量/费用配额及 pgvector 尚待实现。

`evaluation/` 存放实际操作文档对应的初始题集；CLI 支持企业身份执行检索和问答评测。真实企业资料与模型运行待配置，接着开发持久化索引任务、配额和 pgvector。

## 步骤 20：已完成

模型选择与向量索引管理页面、等权 RRF 混合检索、独立聊天模型选择、单轮引用问答与来源显示。下一步为真实模型检索/引用评测，然后建设索引任务队列及容量扩展。详见 [问答接口](ANSWERS.md)。

步骤 23 已完成同步/后台索引统一准入、每日每企业任务额度和页面用量展示；详见 [索引配额](INDEX_QUOTAS.md)。仍未实现 token/费用账本和 pgvector。

步骤 24 已完成持久化企业差异化索引限额、管理员设置页面和审计事件，数据库 0007。下一步是模型调用账本及检索/问答配额，再推进 pgvector；真实模型评测仍待环境配置。

步骤 25 已完成企业模型调用账本与分页管理页面，数据库 0008。下一步模型请求准入与检索/问答配额；随后 pgvector。详见 [模型用量](MODEL_USAGE.md)。

步骤 26 已实现企业每日模型调用准入，检索/问答和索引批次共用调用次数额度。默认 1000/UTC 日，数据库保持 0008；下一步企业差异化模型调用限额与用量汇总，再推进 pgvector。

步骤 27 已补充当日按模型/能力用量汇总，含状态数、已知 token 与未知记录数。数据库保持 0008。下一步企业差异化模型调用限额，后续 pgvector。

步骤 28 已完成持久化企业模型调用限额及管理员页面，数据库 0009。下一步推进 pgvector 扩容与检索基准；真实模型评测仍待环境配置。

步骤 29 接入可选 pgvector 原生精确检索后端与派生缓存，默认仍为 portable。128 分片上限未提升、HNSW 未实现；原生数据库测试仍待 pgvector 环境。下一步先验证原生排序/隔离，再做容量拆分及检索基准。见 [pgvector](PGVECTOR.md)。

步骤 30 已完成真实 pgvector 原生排序/API 隔离/撤回验证，并记录 128/1024 行合成基准。业务仍限制 128 分片；下一步拆分快照元数据与向量存储，验证完整管线后扩容。数据库仍为 0009。

步骤 31 已分离索引元数据读取，pgvector 查询无需加载完整向量 JSON，数据库 0010。128 分片限制不变，下一步文档内容指纹与分批构建扩容。见 [向量元数据](VECTOR_METADATA.md)。
# 当前增量：步骤 32

已完成批次间失效检查、失败保留旧索引和流式指纹编码。下一步：持久化内容修订标识与批次检查点，覆盖恢复、租约和配置变更测试后再提高容量。
# 步骤 33：当前进展

已完成持久批次检查点、内容/模型修订匹配、旧 Worker 写入隔离与成功/失败清理。下一步优化文档生命周期修订计数和独立批次存储，再验证提高 128 分片容量上限。
# 步骤 34：当前进展

已完成内容修订计数与独立批次存储。下一步增加管理端批次进度、恢复状态展示，并测量完整管线容量后再调整上限；当前数据库 0012。
# 步骤 35：当前进展

已完成管理端批次进度与恢复状态展示。下一步建立完整管线容量基准，覆盖解析、构建、恢复、检索和配额，再根据结果调整上限。
# 步骤 36：当前进展

软件管线基准 CLI 和原始结果已落地；下一步增加隔离 PostgreSQL/pgvector 后端和并发场景、内存测量，随后评估提高分片上限。数据库仍为 0012。
# 步骤 37：当前进展

原生并发基准与 tracemalloc 记录已完成。下一步扩展完整 PostgreSQL API/Worker 管线，分别记录无跟踪延迟、Python/数据库 RSS 和并发配额，保持业务上限直到验证完成。
# 步骤 38：当前进展

完整管线已接入隔离 PostgreSQL/pgvector，并验证并发模型额度准入与客户端 RSS。下一步多个成功任务并发、数据库和解析子进程内存及长时间负载；暂不提高容量。
# 步骤 39：当前进展

多成功管线及数据库/解析进程树观测已完成；下一步在同一数据库验证多租户 Worker 竞争、任务公平性和持续负载。短时独立数据库结果暂不支持提高业务上限。
# 步骤 40：当前进展

已完成均衡有限多轮的同库多租户 Worker 基准。下一步不均衡租户、持续入队及等待上限检查，按结果决定调度公平性优化；当前尚无租户轮转。
# 步骤 41：当前进展

不均衡持续入队模式与原始结果已落地。下一步实现可配置租户轮转，保留原策略，并比较高低频租户等待分布；业务容量暂不调整。
