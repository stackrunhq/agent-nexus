# 修改日志

## 步骤 47：游标分页与查询索引（2026-09-15）

- 任务列表增加 next_cursor，按创建时间/ID 续查，绑定资源与筛选条件，保留 offset 兼容。
- 管理页面使用游标栈翻页，条件变化清空位置；新增插入、锚点删除、同秒排序及无效游标测试。
- 0015 增加版本级/模型级排序索引，仍为 24 表；更新迁移、备份、导航与路线图。
- 下一步历史查询基准与执行计划验证。本地提交，不推送；验证结果见 VALIDATION。
- 验证：后端 168 passed、前端 31 passed，TypeScript/Vite、Ruff、0015 独立 wheel 和 Git 空白检查通过。

## 步骤 46：索引任务历史分页与筛选（2026-09-15）

- 管理员任务接口新增 offset/limit/model/status/error，筛选先于分页；保留 data，新增 has_more，默认兼容最近 20 条。
- 管理端接入服务端翻页、状态和精确错误码查询、缺失错误码及清除筛选；取消过期请求，提交后刷新历史。
- 移除旧本地筛选组件，更新文档、路线图与代码导航。数据库保持 0014；下一步游标与查询索引评估。
- 本地 Git 提交，不推送；实际验证结果见 VALIDATION。
- 验证：后端全量 166 passed，前端 31 passed，生产构建、Ruff、独立 wheel 及 Git 空白检查通过。

## 步骤 45：索引失败诊断页面（2026-09-15）

- 增加按精确错误码筛选已加载失败任务，显示中文原因、处理建议和原始任务标识。
- 覆盖超时、限流、模型日额度、授权/凭据、内容变化、无效向量、Worker 中断等；未知错误码安全降级。
- 明确筛选仅覆盖版本最近 20 条任务中的所选模型记录；重新提交沿用现有确认和配额，不自动重试。
- 数据库保持 0014，下一步服务端历史分页与筛选；验证结果见 VALIDATION。仅本地提交，不推送。
- 验证：前端 31 passed，TypeScript/Vite、Ruff、Git 空白检查及隔离 wheel 知识库链路通过。

## 步骤 44：索引 Worker 心跳和策略一致性（2026-09-15）

- 索引 Worker 每 10 秒上报，30 秒有效；正常退出清理，异常中断留下过期观测，上报失败停止领取。
- 管理端展示共享 Worker 的近期、过期、策略不一致数量，区分未知与一致；不将心跳等同任务健康。
- 0014 新增 knowledge_index_workers，共 24 张业务表；更新迁移、备份、代码导航和使用说明。
- 验证：原生 PostgreSQL/pgvector 全量 165 passed、0 skipped，前端 28 passed，Ruff、TypeScript/Vite、独立 wheel 0014 与实际 Worker 启停通过。
- 下一步完善索引失败诊断和处理建议。仅本地提交，不推送。

## 步骤 43：管理端调度配置与租户等待指标（2026-09-15）

- 验证：后端定向 8 passed，前端全量 27 passed，Ruff、TypeScript/Vite 和 Git 空白检查通过。
- index-usage 新增 scheduling 聚合，显示 API 策略、排队/处理中/过期租约数量及最早等待秒数，按租户隔离；无任务返回 null 等待值。
- 管理端展示统计时间、企业全范围及 Worker 尚未核验提示；保留手动刷新，不展示无依据的历史耗时或预计完成时间。
- 新增隔离/空队列/租约/终止清理接口测试和零值/null 页面回归。数据库保持 0013；下一步 Worker 心跳和配置一致性检查。
- 仅本地提交，不推送。

## 步骤 42：可配置租户轮转调度（2026-09-15）

- 验证：PostgreSQL 全量 160 passed、0 skipped，定向 8 passed；策略比较 243 项任务成功；Ruff、Compose config、独立 wheel 0013 通过。
- 新增 NEXUS_INDEX_SCHEDULER=fifo|tenant_round_robin，默认保持 FIFO；轮转按可领取租户排序循环，租户内保持时间/ID 顺序。
- 0013 增加 knowledge_index_scheduler，共 23 张业务表；游标与领取同事务更新，多个 Worker 共享，失败回滚。
- API 校验配置，Compose API/index-worker 注入变量；保持租约、重试、检查点和同步构建行为。增加顺序/并发/回滚/升级测试及同负载策略比较。
- 更新升级、备份、导航和路线图；下一步调度状态及租户等待指标展示。仅本地提交，不推送。

## 步骤 41：不均衡租户持续入队（2026-09-14）

- shared_queue_benchmark 新增 --continuous，生产者与 Worker 同时运行；高频租户每周期提交 4 版本，其他租户每 5 周期各提交一次。
- 比较 1/4 Worker、30 周期，记录唯一任务计数和最长等待；活跃任务合并保留首次提交计时，补齐生产结束与空队列判定的竞态保护。
- 临时 PostgreSQL 实测全部唯一任务完成，无重复领取或遗留检查点；原均衡模式一轮兼容检查通过。保存原始结果，Ruff 通过。
- 有界短时观测不保证长期公平性；下一步比较可配置租户轮转策略，不改变当前生产调度或容量。仅本地提交，不推送。

## 步骤 40：同库多租户 Worker 多轮竞争（2026-09-14）

- 新增 shared_queue_benchmark CLI，4 租户 × 2 版本共享同一个 PostgreSQL 队列，比较 1/4 个异步 Worker，各消费 10 轮共 80 项任务。
- 两组合计 160 项全部成功且恰好领取一次，每组每租户 20 项，无遗留检查点，版本检索来源正确；重复提交复用活跃任务。
- 单 Worker 每轮消费中位约 624 ms，四 Worker 约 283 ms。当前按时间和 ID 排序、无租户轮转，均衡有限负载不代表长期公平性。
- 保存原始结果、验证边界和导航；Ruff 通过，临时数据库和凭据清理。保持 128 分片上限，仅本地提交，不推送。

## 步骤 39：多成功管线与进程树内存（2026-09-14）

- 新增 pipeline_concurrency CLI，启动 2/4 个独立 PostgreSQL 管线进程，每个 128 分片，六个管线全部通过构建、恢复、检索问答和额度检查。
- 采样管线、解析后代及 PostgreSQL 进程树；修正 Windows 虚拟环境启动器分组。四并发三组采样峰值分别约 406.74/67.01/407.55 MiB，共享页可能重复计数。
- 保存原始结果和命令边界。进程枚举/RSS/异常数据库清理相关 4 项测试、Ruff 通过；临时数据库、实例与凭据清理完成。
- 独立数据库、短时模拟模型结果不代表共享任务队列容量；保持 128 上限，下一步同库多租户 Worker 竞争和持续负载。仅本地提交，不推送。

## 步骤 38：PostgreSQL 完整管线、并发配额与工作集（2026-09-14）

- 新增 postgres_pipeline CLI，为每个场景创建随机隔离数据库并自动清理；复用软件管线并启用 pgvector，补充四请求竞争一次模型额度检查。
- 临时 PostgreSQL 17.11 + pgvector 0.8.6 的 16/64/128 场景全部通过，129 分片模型调用前拒绝；每组配额竞争恰好一个 200、三个 429。
- Windows 每 20 ms 采样 Python 工作集，128 分片场景采样最大约 132.16 MiB；不包含数据库和解析子进程。记录原始结果，保持业务上限。
- 新增异常清理两项测试；Ruff 通过。更新命令、验证和导航，仅本地提交，不推送。

## 步骤 37：pgvector 并发与 Python 分配基准（2026-09-14）

- 新增 vector_concurrency CLI，128/1024 行、1/2/4 线程并发，独立版本缓存和确定性参考结果，记录每线程延迟及整组 tracemalloc 峰值。
- 在临时 PostgreSQL 17.11 + pgvector 0.8.6 实跑六组，Top-10 均 100% 一致；1024 行四线程 Python 分配峰值约 25.4 MiB。保存原始 JSON；数据库已停止、凭据清理。
- 测量仅覆盖原生缓存层，内存不含数据库，开启跟踪会影响延迟；保持 128 业务分片上限。下一步完整 PostgreSQL API/Worker 管线及进程 RSS 测量。
- 仅本地 Git 提交，不推送。

## 步骤 36：可重复的软件管线容量基准（2026-09-14）

- 新增 pipeline_benchmark CLI，独立临时 SQLite 覆盖上传、解析、发布、索引任务、中断恢复、词法/向量/混合检索及引用问答、配额拦截。
- 保存 16/64/128 分片实测原始 JSON；全部复用首批 16 分片，129 分片模型调用前拒绝。使用确定性 HTTP 模拟模型，不连接真实服务。
- 128 分片本机构建约 444 ms、混合检索中位约 71 ms；单次构建和少量查询不能代表生产容量。保持 128 上限，下一步 PostgreSQL/pgvector 并发基准。
- 更新结果解释、命令和代码导航；仅本地提交，不推送。

## 步骤 35：管理端索引进度与恢复状态（2026-09-14）

- 验证：后端相关 10 passed，前端全量 26 passed，Ruff、TypeScript/Vite 和 Git 空白检查通过。
- 任务列表新增 saved_batches 和 recovery_state，聚合统计已持久化批次，不读取向量正文；补齐企业/应用/版本归属校验。
- 管理端展示已保存批次、租约到期等待领取、重新处理、完成及终止提示；不将领取次数解释为实际复用，不估算完成百分比，保持手动刷新。
- 新增接口计数/租约/隔离及页面状态转换测试。数据库保持 0012，无迁移；下一步完整管线容量基准。仅本地提交，不推送。

## 步骤 34：内容修订计数与独立索引批次（2026-09-14）

- 验证：PostgreSQL 全量 151 passed、0 skipped；Ruff、Git 空白检查、独立 wheel 0012 业务链路通过。
- 0012 新增版本内容修订表和独立批次表，共 22 张业务表；解析完成、发布及撤回与计数同事务更新。
- 构建批次间以计数和权限校验替代全文读取，开始及最终阶段仍校验完整内容；最终保存再次验证计数。
- 检查点每批独立追加，验证连续性、维度、修订、租约及累计容量；恢复按序合并，成功/失败原子清理。
- 升级清除旧格式未完成检查点，保留任务与已完成索引；旧任务恢复时从头构建一次。仍限制 128 分片，下一步管理端进度展示和完整管线容量基准。仅本地提交，不推送。

## 步骤 33：索引批次检查点与中断恢复（2026-09-14）

- 验证：PostgreSQL 全量 147 passed、0 skipped；定向 20 passed；Ruff、Git 空白检查和独立 wheel 0011 业务链路通过。
- 0011 增加 knowledge_index_checkpoints，共 20 张业务表；保存任务级内容/模型修订摘要及已完成向量前缀。
- Worker 中断并重新领取后复用匹配检查点，内容或模型变化则重新构建；有效租约及令牌控制读写，拒绝旧 Worker。
- 成功发布和检查点清理同事务；普通失败及重试耗尽清理进度。旧索引不受中途失败影响，已发生的上游调用仍计入用量。
- 更新部署、迁移、备份与代码导航。同步接口兼容，保持 128 分片上限；下一步优化内容修订计数与独立批次存储。仅本地提交，不推送。

## 步骤 32：分批构建失效检查与流式指纹（2026-09-14）

- 每批 16 个分片之间重新校验发布内容、企业模型授权及模型配置；发生变化后停止后续调用，保留已有索引和元数据。
- 使用流式 JSON 编码计算内容指纹，避免一次生成整个 JSON 字符串及字节副本；编码格式与旧索引完全兼容，无需迁移或重建。
- 新增多批次成功、撤回、撤销授权、配置变更及指纹兼容测试。数据库保持 0010，128 分片上限保持；本步不包含断点续建。
- 仅本地 Git 提交，不推送。

## 步骤 31：索引元数据拆分（2026-09-14）

- 新增 0010 迁移及 knowledge_vector_metadata 表，共 19 张业务表；为旧索引回填分片数和快照摘要。
- 索引状态及 pgvector 检索使用轻量元数据，避免读取完整向量 JSON；构建时与快照、原生缓存和任务结果同事务更新。
- PostgreSQL 全量回归 138 passed，补充原生迁移/API 定向 4 passed；覆盖 SQLite 回填、企业隔离、撤回失效与无完整向量读取。
- 更新升级、部署和代码导航；128 分片限制保持，下一步优化内容指纹校验与分批构建。仅本地提交，不推送。

## 步骤 30：原生 pgvector 验证与可重复基准（2026-09-11）

- 在独立临时目录使用现有 C++ 工具编译官方 pgvector 0.8.6，运行 PostgreSQL 17.11 原生测试；完整回归 135 passed、0 skipped，新增 API 原生集成后定向 4 passed。
- 验证原生排序、快照绑定、企业隔离、撤回失效以及 SQLite 导入后的原生/portable 结果一致。
- 新增 vector_benchmark CLI 和 128/1024 行、64 维、10 轮实测 JSON；Top-10 重合率均为 100%，查询中位数 2.45/15.35 ms。仅为合成缓存层基准，不代表完整业务容量。
- 临时数据库停止、凭据删除；默认 128 分片限制保持，未实现 HNSW 或 Docker 运行验证。仅本地提交，不推送。

## 步骤 29：可选 pgvector 后端基础（2026-09-11）

- 新增原生 vector 派生缓存、数据库余弦排序、快照摘要隔离与显式初始化 CLI；默认 portable 不变，业务数据库仍为 0009。
- 原生缓存与快照/任务结果同事务更新；缺失或旧缓存要求重建。新增 Compose 可选覆盖配置和部署/回退说明。
- PostgreSQL 默认后端完整回归 134 passed、1 skipped；跳过的是未配置环境的原生 pgvector 集成测试。Ruff 和 Compose 配置通过，前端未修改。
- 128 分片上限未提高，无 HNSW/IVFFlat；未验证原生 SQL、扩容性能或 Docker 实际运行，不声称大规模扩容完成。仅本地提交，不推送。

## 步骤 28：企业差异化模型调用限额（2026-09-11）

- 0009 新增 tenant_model_quotas，共 18 表；企业每日模型调用覆盖值持久化，null 继承默认，0 暂停新调用。
- 管理员 GET/PUT model-quota 与确认保存页面；修改和准入共用企业锁，保留既有用量，写企业审计事件，API/Worker 共用覆盖值。
- PostgreSQL 全量 132 passed、前端 24 passed、Ruff、TypeScript/Vite、18 表恢复和独立 wheel 验证通过；未运行真实模型费用、Docker 容器或浏览器视觉验证。
- 下一步 pgvector 扩容与检索基准；仅本地提交，不推送。

## 步骤 27：企业当日模型用量汇总（2026-09-11）

- model-usage 增加按模型/能力分组的当日调用、成功/失败/pending、已知 token 合计及未知记录数，页面同步展示。
- 统计按企业及 UTC 日隔离；全部未知保留 null，部分未知明确提示不是完整用量。数据库保持 0008。
- PostgreSQL 全量 130 passed、前端 23 passed、汇总定向测试、Ruff、TypeScript/Vite 和独立 wheel 通过；未执行真实费用对账或浏览器视觉验证。
- 下一步企业差异化模型调用限额，再推进 pgvector。仅本地提交，不推送。

## 步骤 26：企业每日模型调用准入（2026-09-11）

- 基于账本原子登记每日调用额度，默认每企业 1000 次；NEXUS_MODEL_DAILY_LIMIT 支持 0–1000000，API 与索引 Worker 同配置。
- 直接模型调用、索引批次、检索和问答共用额度；失败/pending 计数，超额不调用上游。新增 model-usage 查询及页面用量展示。
- PostgreSQL 全量 129 passed、前端 23 passed；并发准入/隔离/跨日/上游拦截、Ruff、TypeScript/Vite、Compose 配置及独立 wheel 通过。数据库保持 0008。
- 本轮按调用次数限制，不是 token、费用或独立业务请求额度；真实模型、Docker 实际启动与浏览器视觉未验证。仅本地提交，不推送。

## 步骤 25：企业模型调用账本（2026-09-11）

- 0008 新增 model_calls，共 17 表；记录企业模型调用的 pending/成功/失败、配置指纹、请求 ID、耗时及上游 token，不保存正文、向量或密钥。
- 覆盖直接 chat/embedding、索引批次、检索和引用问答；未知 token 保留 null，中断保留 pending，不推断费用。新增管理员分页接口与调用账本页面。
- PostgreSQL 完整回归 127 passed、前端 23 passed；Ruff、TypeScript/Vite、17 表恢复、独立 wheel 0008 和账本链路通过。真实模型费用、浏览器视觉和 Docker 实际启动未验证。
- 下一步实现模型请求准入及检索/问答配额；本轮不声称已限制模型调用或完成 pgvector。更新日志与操作文档，仅本地提交，不推送。

## 步骤 24：企业差异化索引限额（2026-09-11）

- 0007 新增 tenant_index_quotas，持久化每日/活跃任务覆盖值；null 继承默认，0 暂停新任务，修改不清空用量也不取消旧任务。
- 管理员 GET/PUT index-quota、页面读取和确认保存；修改与任务准入共用锁，写企业审计事件，index-usage 显示生效配置并拒绝不存在企业。
- PostgreSQL 完整回归 124 passed、前端 22 passed，Ruff、TypeScript/Vite 通过；16 表备份恢复与 0006 显式升级验证通过。真实模型质量与浏览器视觉未验证。
- 更新迁移、部署、备份、代码导航和后续清单。下一步模型调用账本与检索/问答配额，pgvector 尚未实现；仅本地提交，不推送。

## 步骤 23：统一索引准入与每日任务额度（2026-09-10）

- 同步构建接入持久任务登记、活跃数限制及租约写入校验，保留原返回格式；相同版本/模型活跃时拒绝同步重复构建。
- 每企业每日默认 100 个新任务，UTC 零点重置；NEXUS_INDEX_DAILY_LIMIT 配置统一上限。原子准入防并发超额，重复任务不重复计数，失败任务仍计数。
- 新增管理员 index-usage 接口与页面用量展示。数据库保持 0006。额度是任务数量，不包括 token/费用或检索/问答调用。
- PostgreSQL 完整回归 122 passed、前端 21 passed，配额定向复核 4 passed；Ruff、TypeScript/Vite、Compose 配置与独立 wheel 验证通过。真实模型质量、Docker 运行和浏览器视觉未验证。
- 更新操作与下一步清单，仅本地提交，不推送。

## 步骤 22：持久化后台索引任务（2026-09-10）

- 0006 新增任务表；管理员提交/查询接口，页面异步入队并按需刷新状态。同版本/模型活跃任务复用，每企业最多 5 项活跃任务。
- 独立 `--queue indexes` Worker，300 秒租约、最多 3 次中断恢复；有效租约校验、索引写入和任务成功状态处于同一事务，失效 Worker 不可覆盖结果。执行时复查模型与资料。
- Compose 增加 index-worker 和 PostgreSQL 迁移依赖；原同步接口保留兼容，不受队列容量限制，本轮不声称完成企业总用量配额或 pgvector。
- 完整 PostgreSQL 回归 118 passed，前端 21 passed；Ruff、TypeScript/Vite、Compose 配置、独立 wheel 0006 与后台消费通过。真实模型、Docker 实际启动和浏览器视觉未验证。
- 更新迁移、备份、接口与导航文档；仅本地 Git 提交，不推送。

## 步骤 21：CSS 类型声明与手册评测基础（2026-09-10）

- 补充 Vite 资源类型声明，CSS 导入保持原写法；TypeScript/Vite 构建通过。
- 新增 evaluation 目录，以项目实际操作手册建立 5 题初始来源/拒答评测集；CLI 使用企业测试凭据调用混合检索或引用问答，记录来源命中、拒答、延迟和题集摘要，不保存原文或回答。
- Ruff、评测器自动化测试通过。尚未运行真实企业手册与模型质量评测，需配置测试环境和已授权模型；索引后台任务、配额和 pgvector 为后续独立开发步骤，未在本轮实现。

## 步骤 20：模型索引管理、混合检索与引用问答（2026-09-10）

- 知识库页面按企业授权和能力筛选本地/云端模型，提供索引状态、建立/重建确认、维度和分片数；切换模型或关闭范围时取消旧请求。
- 新增企业及管理员 `/hybrid-search`、`/answers`，关键词与向量采用等权 RRF 合并；聊天与 embedding 模型分别选择，固定聊天配置并复查授权和已发布内容。
- 单轮问答校验引用编号与正文标记，返回可核对的原文来源；无引用拒答、非法引用拒绝输出。纯文本展示，尚不包含语义事实验证、会话或流式输出。
- 完整 PostgreSQL 回归 111 passed；随后补充缺失索引/生成中撤回测试，问答定向 10 passed。前端 21 passed、TypeScript/Vite、Ruff 与独立 wheel 混合检索/引用问答通过。数据库仍为 0005；真实模型质量、浏览器视觉和 Docker 实际启动未验证。
- 更新操作接口、代码导航、部署说明及下一步清单；仅提交本地 Git，不推送。

## 步骤 19：持久化向量快照与检索 API（2026-09-10）

- 新增管理员建立/重建、状态查询、预览及企业向量检索接口，模型必须已向企业授权；支持网关已接入的云端兼容 API、本地兼容 API 和 Ollama embedding。
- 0005 新增向量快照表，14 表迁移、就绪检查、导入与备份恢复适配；关键词和向量共享可读分片查询，向量保存归一化结果、模型配置及内容指纹。
- 分批固定同一模型配置，失败保留旧快照；文档撤回、模型配置变化要求重建，模型等待结束再次核验权限和指纹。
- 当前同步、有界精确余弦检索：最多 128 分片、4096 维、16 MiB JSON，嵌入批次总限时 120 秒。尚无 pgvector、后台索引队列、管理页面接入或混合检索。
- PostgreSQL 17.11 全量 103 passed、0 skipped，14 表恢复与受限角色向量读取、Ruff、独立 wheel 全链路通过；模型为模拟上游，未声称真实检索质量。临时数据库停止、凭据删除。前端未变，Docker/浏览器未运行。
- 更新升级、接口、代码导航和下一步清单；仅本地提交，不推送。

## 步骤 18：带发布权限过滤的关键词检索（2026-09-10）

- 新增企业 search API 和管理员同权限预览；SQL 在读取分片时校验企业、启用状态、产品版本与文档发布条件，撤回后下次查询不再返回内容。
- NFKC/大小写归一化、常用汉字单字与双字、英文数字词项，BM25 方式排序；返回文件、摘要、版本、分片、页码/段落和字符来源，无命中返回空结果。
- 页面接入关键词检索和原文来源展示，取消页面请求及清除发布状态变化后的结果；不生成大模型回答。
- 查询最多 200 字符、返回 1–20 项；查询范围超过 5000 个分片或 400 万字符明确拒绝，不静默截断。当前为实时有界关键词扫描，不是向量索引，不调用模型，数据库仍为 0004。
- 验证：PostgreSQL 17.11 全量 99 passed、0 skipped，含恢复后个人企业身份检索；前端 18 passed、TypeScript/Vite、Ruff 和独立 wheel 上传/解析/发布/检索验证通过。前端测试的整表样式扫描导致超时，改为目标文本定位后保持原超时通过。
- 临时 PostgreSQL 已停止、凭据已删除；未验证真实浏览器、Docker 容器、真实模型和规模性能。下一步是模型配置绑定的持久化向量索引、混合检索和引用问答。仅本地提交，不推送。

## 步骤 17：知识库管理页面（2026-09-10）

- 在产品版本列表增加“管理知识库”，按企业/应用/版本接入上传、处理状态、失败重试、文档与分片服务端分页、发布/撤回确认。
- 支持 PDF/DOCX/Markdown/TXT 文件选择与非空、大小、文件名校验；显示解析错误及无文本页提示，分片按纯文本展示页码/段落和字符偏移。
- 管理员客户端新增原始二进制上传与局部取消；切换归属、卸载和断开连接后取消请求，拒绝迟到响应；非 JSON 的 401 也能清理个人会话。
- 状态按需刷新；发布产品版本后需重新进入知识库单独发布文档。数据库仍为 0004，不增加迁移，不修改 Worker。
- 前端 16 项测试、TypeScript/Vite 构建、后端页面测试及 Ruff 通过；独立 wheel 验证最新页面资源逐字节一致、上传和真实解析子进程成功。构建保留 Ant Design 指令及约 965 KB 大块提示。
- Browser 技能连接检查未发现可用浏览器，未执行真实视觉交互；未重跑 PostgreSQL 全量或 Docker 容器。本轮仅本地提交，不推送；下一步开发向量索引、权限过滤检索和引用问答。

## 步骤 16：知识库入库、独立 Worker 与发布读取（2026-09-09）

- 新增知识库上传、分页元信息/分片、失败重试、文档发布/撤回和企业读取 API；仅管理员可写，上传绑定启用应用的 draft 产品版本。
- 原文件与任务原子存入数据库；同版本/文件名/摘要上传去重。0004 新增 knowledge_documents、knowledge_chunks 两表，迁移检查/导入/恢复支持 13 表与二进制摘要。
- 独立 Worker 用领取凭证和 5 分钟租约防止重复领取及旧结果覆盖；解析子进程 60 秒超时、POSIX 资源限制，全部分片和完成状态原子落库。中断可恢复，3 次中断后进入失败，支持人工重试。
- 文档 ready 不自动发布；只有本企业启用应用、已发布产品版本和已发布 ready 文档可读；撤回/退役后隐藏，写操作记录操作人及请求 ID。
- Compose 新增受资源限制的 worker 服务，原生提供 nexus-worker / python -m agent_nexus_cli.worker；同步升级、接口、目录和下一步文档。管理页面、S3/Celery、OCR、检索和配额后续实现。
- 验证：PostgreSQL 17.11 全量 95 passed、0 skipped；13 表备份恢复及受限角色文档读取、Ruff、两套 Compose 配置和独立安装包上传/子进程解析通过。临时数据库停止、凭据删除；Docker 引擎未运行，容器和 POSIX 资源限制未实测，前端未改动。仅本地提交，不推送。

## 步骤 15：知识库离线解析与来源分片（2026-09-09）

- 新增 knowledge 业务模块：UTF-8 TXT/Markdown、文本型 PDF、DOCX 正文与表格段落解析。
- 字符窗口分片保留来源类型、页码/段落序号和字符偏移，限制重叠比例；支持中文且可按偏移追溯原文。
- 新增 nexus-document / python -m agent_nexus_cli.document JSON 预览命令、SHA256 和稳定错误码；不执行入库或模型调用。
- 增加文件、页数、文本及 DOCX 包大小限制；拒绝加密 PDF/XML 实体，报告无文本页。PDF 进程资源隔离需在上传阶段实现。
- 新增解析与 CLI 回归，更新依赖哈希锁、功能状态和代码导航。上传、持久化、Worker、管理页面、OCR 和检索待后续开发；仅本地提交。
- 验证：后端 86 passed、2 skipped（PostgreSQL 未配置），Ruff 与独立 wheel 解析/CLI 检查通过；未运行 Docker、实库和前端交互。

## 步骤 14：企业应用与版本管理（2026-09-09）

- 新增 applications 业务模块与 React 面板：按企业创建/启停应用、创建版本草稿、发布/退役和操作记录。
- 企业读取接口绑定可信身份，只返回本企业启用应用及已发布版本；跨企业路径返回 404，无企业归属的 bootstrap 客户端拒绝访问。
- 应用标识和版本标签按归属唯一，版本只允许 draft → published → retired；重复状态请求不重复审计，事务锁验证并发发布一致性。
- 0003 新增应用、版本和事件三表，迁移/就绪检查/导入/序列校正及受限数据库角色适配 11 表。
- PostgreSQL 17.11 全量 73 passed、0 skipped，含应用版本恢复和受限角色读取；前端 9 项通过，类型检查、构建、Ruff 与独立 wheel 验证通过。
- 更新目录导航、升级文档和功能状态。PDF/DOCX 等解析、OCR、分片和检索尚未实现，下一步进入文件入库。
- 临时 PostgreSQL 已停止并移除生成凭据；未运行 Docker 容器、真实模型、真实浏览器视觉检查或远端 CI；仅本地提交，不推送。

## 步骤 13：个人账号、会话与角色（2026-09-09）

- 新增 identity 模块：个人登录、当前身份、退出，账号创建/启停/密码重置与安全事件。
- 平台管理员可管理后台；企业成员只能调用所属企业授权模型，bootstrap 模式也不能绕过个人企业权限。
- scrypt 随机盐密码摘要、随机会话摘要、1 小时有效期、每账号单会话、登录失败锁定；停用和重置撤销会话。
- React 加入个人登录与账号面板，退出和到期清除页面状态；模型配置审计记录个人操作人 ID。
- Alembic 0002 新增三张身份表；旧版本显式升级，导入、序列校正、健康检查和受限角色授权扩展至八表。
- PostgreSQL 17.11 全量回归 69 passed、0 skipped，含个人凭据/会话恢复；前端 8 项通过，TypeScript、构建及 Ruff 通过。账号列表布尔类型校正后定向回归通过。
- 独立 wheel 校验迁移、个人创建/登录/退出、管理权限、页面与资源通过。临时 PostgreSQL 已停止，生成凭据文件已移除。
- 完整组织角色、自助改密、MFA/SSO、文档权限及原企业事件个人归因仍待完成；不推送远程。

## 步骤 12：React 企业管理页面（2026-09-09）

- 最终 wheel 独立验证迁移、健康、模型接口、企业页及 JS/CSS 均通过；限定 Python 包发现，避免把 TypeScript 功能目录作为命名空间包安装。
- 引入 React、TypeScript、Vite、Ant Design 与 npm 锁文件；功能位于 web/src/app/features/tenants。
- 新增 /admin/tenants：企业创建、启停、轮换、模型授权/撤销及最近事件；模型工作台增加入口。
- 管理员令牌只在内存；断开取消请求，凭据弹窗关闭立即卸载，轮换和启停需确认。
- 接口客户端支持 204，携带请求 ID 的错误提示；bootstrap 模式明确提示调用授权尚未启用。
- Docker 增加 Node 构建阶段，CI 增加前端检查；企业页面单独放宽内联样式，脚本仍限定同源。
- 后端 60 passed、2 skipped；前端 5 项通过，类型检查和生产构建通过。未启动 PostgreSQL、Docker 或真实模型，未运行远端 CI。
- 个人登录、角色权限仍待开发；仅本地 Git 提交，不推送。

## 步骤 11：顶层 API、前端、CLI 与部署分层（2026-09-09）

- 后端及测试迁至 api/；页面迁至 web/；数据库工具迁至 cli/；容器配置集中 docker/；image/ 明确图片资源职责。
- 三个源码目录统一打包，资源通过包接口定位；新增 nexus-db，保留原应用及数据库命令。
- 更新构建、Compose、CI 和目录导航；明确 React/TypeScript、Worker 等目标技术仍待开发。
- 59 项回归通过，2 项 PostgreSQL 测试跳过；Ruff、JS 语法、新旧 CLI 帮助、两套 Compose 配置解析通过。
- 干净 wheel 构建及独立包迁移、健康、页面资源、模型列表验证通过。
- Docker 配置解析有用户配置读取警告；未运行容器、真实模型或远端 CI。仅本地提交，不推送。

## 步骤 10：业务目录与代码导航（2026-09-09）

- 按 core/api/models/tenants/storage/web 分包，拆分模型管理与调用路由，app.py 聚焦应用组装。
- 抽出公共配置、错误及请求基类，企业模块不再依赖模型网关的异常实现。
- 测试按业务归档，更新 CI 静态资源路径、包资源配置与恢复命令。
- 增加 docs/README.md 与 architecture/CODE_MAP.md，记录职责、阅读顺序、修改入口和内部导入映射。
- 保留 HTTP、环境变量、数据库结构与启动/数据库 CLI 命令。外部脚本若导入旧内部模块需调整路径。
- 验证：59 passed、2 skipped（未启动 PostgreSQL），Ruff 和 JS 语法检查通过；清理旧构建缓存后 wheel 构建、包独立导入、SQLite 迁移、页面资源与 API 冒烟通过。
- 本轮优先完成结构重构，企业页面、用户登录与角色尚待开发；仅本地 Git 提交，不推送。

## 2026-09-09 · 步骤 09 · PostgreSQL 实库与恢复演练完成

- 准备独立 PostgreSQL 17.11，执行此前跳过的迁移、导入、企业授权和并发测试。
- 新增真实 pg_dump/pg_restore 测试，恢复到全新数据库，核对五表摘要、迁移版本、凭据/授权、健康及对话 API、后续审计序列。
- 验证受限运行角色可执行业务读写，但不能建表或修改迁移版本。
- 完整回归 61 项通过、0 跳过；Ruff 通过。上游模型使用模拟服务，不是模型联调。
- CI 增加匹配版本的 PostgreSQL 客户端安装步骤；新增 BACKUP_RESTORE.md 并更新进度。
- 临时集群已停止，测试库和角色已清理、生成凭据文件已移除；不推送远端。
- 仍未完成 Docker 实际运行、生产规模 RPO/RTO/PITR 和自动备份。

## 2026-09-09 · 步骤 08 · 数据库健康与故障诊断优化

- 就绪检查改为零行表结构查询及版本核验，不再加载和解析全部模型配置。
- 区分存活与就绪，数据库未就绪返回 503 database_not_ready；数据库操作错误统一脱敏并返回请求 ID。
- 新增 db_cli check，显示数据库类型及迁移版本，不创建缺失 SQLite 文件。
- PostgreSQL 连接池等待和事务锁等待各限制为 5 秒；尚未进行真实 PostgreSQL 超时测试。
- 验证：59 项测试通过，1 项 PostgreSQL 测试因无服务跳过，Ruff 通过。
- 下一步仍为 PostgreSQL 实库与恢复演练、企业页面及用户角色；本轮不宣称完成这些功能。

每个可验收步骤更新此文件，检查通过后创建本地 Git 提交。未经用户要求不推送远端。已存在的未提交实现作为一个基线提交保存，不伪造历史拆分。

## 2026-09-08 · 步骤 01–02 · 模型网关与工作台基线

- 统一云端兼容接口、本地 vLLM 兼容接口、Ollama 原生对话及向量。
- 模型配置、环境变量凭据引用、管理员/调用端令牌分离。
- 浏览器管理工作台：编辑、启停、试调用及耗时/用量展示。
- 参数能力校验、总时限、响应大小限制和错误脱敏。
- Docker、原生启动、CI、依赖锁文件及分阶段路线图。
- 验证：32 项 Python 测试、Ruff、JavaScript 语法检查通过，wheel 构建成功。
- 限制：真实模型、Docker 启动和浏览器交互尚未实测；当前不具备多租户隔离。

后续提交可通过 `git log --oneline` 查看，每次提交同时包含对应代码与日志。

## 2026-09-08 · 步骤 03 · 模型配置审计 API

- 配置创建、修改、启停写入持久化审计，记录 UTC 时间、变更字段、操作者类别和请求 ID。
- 配置和审计同事务提交；重复保存相同配置不新增记录。
- 管理员专属日志接口支持按别名筛选和游标分页；不记录字段值、密钥或提示词。
- 验证：36 项测试和 Ruff 通过，覆盖事务回滚、重启保留、权限及分页。
- 当前共享管理员令牌只标记 platform_admin，尚不能识别具体个人；既有配置不补造历史审计。

## 2026-09-08 · 步骤 04 · 工作台修改记录

- 工作台增加修改记录表，支持按模型别名查询及加载更早记录。
- 保存或启停模型后刷新记录；断开连接清除已加载记录。
- 日志内容以纯文本展示，分页查询处理过期响应，避免旧查询覆盖新筛选。
- 新增 CONTRIBUTING.md，明确每步更新日志、验证、暂存、创建本地提交，不推送。
- 验证：36 项回归测试、JavaScript 语法和 Git 空白检查通过；浏览器视觉与点击交互尚未实测。

## 2026-09-08 · 步骤 05 · 配置并发保护

- 新增单模型读取接口，管理列表、读取和保存返回配置 ETag。
- 新建要求 If-None-Match: *，更新要求精确 If-Match；缺少条件返回 428，冲突返回 412。
- 在配置与审计事务内校验快照，防止旧页面覆盖；冲突不写入配置和审计。
- 工作台编辑、启停和新建自动携带条件；冲突保留表单并提示重新加载，不自动覆盖。
- 规范化默认数值后计算摘要，保证保存、读取及重启后的相同配置一致。
- 验证：40 项测试、Ruff、JavaScript 语法检查通过；覆盖真实双线程竞争、重复保存和条件头校验。
- 接口兼容性：旧管理写入脚本必须增加条件头，提交正文去掉只读 etag；对话和向量调用不受影响。
- 浏览器交互、真实模型及 Docker 实际启动尚未实测。

## 2026-09-08 · 步骤 06 · 企业模型接入基础（0.3）

- 新增企业创建、启停、机器凭据一次性签发/轮换、模型授权/撤销与最近事件 API。
- 企业密钥为服务端生成的随机凭据，数据库只保存摘要；轮换使旧密钥失效。
- 新增 tenant 鉴权模式，企业只能列出和调用授权模型；全局开发令牌不能绕过企业模式。
- 保留默认 bootstrap 开发模式，工作台明确展示当前模式；Compose 支持模式切换。
- 新增 STATUS.md 汇总已完成和下一步，TENANT_ACCESS.md 说明启用、授权及接口边界。
- 验证：48 项测试、Ruff、JavaScript 语法、Compose 配置与 Git 空白检查通过。
- 未完成：PostgreSQL、企业页面、员工/角色、知识库及会话隔离、配额限流；真实模型、容器运行及浏览器交互未实测。

## 2026-09-08 · 步骤 07 · 双数据库存储与迁移

- 模型、企业和审计存储改用 SQLAlchemy，保留 SQLite 开发方式并接入 PostgreSQL 驱动。
- PostgreSQL 采用按模型/企业的事务锁，保持条件更新及审计原子性；数据库查询不阻塞异步模型请求事件循环。
- Alembic 初始版本 0001、显式升级命令、旧 SQLite 结构登记、SQLite 只读导入及逐表核对，拒绝非空目标和同源导入。
- PostgreSQL Compose 按数据库健康 → 迁移成功 → API 启动；迁移脚本随 wheel 打包。
- 更新锁定依赖、CI PostgreSQL 专用测试服务、DATABASE.md 和当前功能清单。
- 验证：55 项通过、1 项实库测试因无服务跳过；Ruff、脚本语法、Compose 配置和 wheel 构建通过。
- 尚未验证真实 PostgreSQL、容器运行、生产备份恢复及浏览器交互；不能把跳过测试表述为通过。
